# 2026-02-09 工作日记

## 完成事项

### 产品需求池完善

基于 `docs/01-客户资料/` 中的三个场景资料和两次周会纪要，逐条完善了 `docs/02-需求分析/漕河泾需求池_产品需求池.csv`。

#### 主要工作内容

1. **上下文平台模块（21条）**：补充了所有数据接入项的需求描述、优先级、版本号和状态
   - 飞书数据接入（妙记/日历/文档/任务/多维表格）：P0-P1，版本0.1-0.2
   - 内部数据接入（烽火CRM/会议室/访客/门禁）：P1-P3，烽火尚未启用
   - 外部数据接入（企查查/知识产权/投融资）：P1，版本0.1-0.2
   - 数据库建设（企业数据库/部门知识库）：P0-P1，企业数据库是三个场景共用底座

2. **客户拜访app模块（45条）**：补充了面向对象、需求版本、优先级、comments和需求状态
   - 企业画像/背调报告/沟通清单：P0，版本0.1
   - 拜访记录（妙记关联/记录生成/调整/关键问题检测）：P0-P1，版本0.2
   - 需求处理模块：P1-P2，版本0.3，拜访流程第四步，优先级低于前三步
   - 走访管理看板：P2，版本0.3，顾总暂缓
   - 走访数据接入/配置：P0-P1，版本0.1-0.2

3. **孵化器管理app模块（16条）**：补充了所有缺失的需求描述、优先级和状态
   - 日常运营监控：P0，赵婧总排序第一
   - 资源与订单智能匹配：P0，核心AI能力体现
   - 订单匹配Agent（三个子query）：P0，版本0.1
   - 关系网可视化：P1，版本0.2，唯一前端展示页面
   - 奇岱松人流/门禁：P3延期，需硬件部署

4. **领导看板app模块（2条）**：补充了需求描述，标记为P2延期

5. **政策服务app模块（34条）**：补充缺失的版本号、优先级、comments和状态
   - 一期聚焦高新技术企业认定
   - 申报材料填写：P3延期到二期
   - 政策审核结果监测：P3延期，合规性待确认
   - 走访必问问题生成：P0，薛科总强调最高优先级

#### 关键决策依据

- **版本规划**（来自0209周会）：Demo → 0.1(2/24-3/6) → 0.2(3/7-3/20) → 0.3(3/21-4/3)
- **优先级逻辑**：P0=4月必须交付 / P1=重要可简化 / P2=可延后 / P3=二期+
- **需求状态**：待开发（已确认做的）/ 待确认（需顾总拍板）/ 延期（推后）

#### 不确定点（需FDE确认）

1. 烽火CRM的对接方式：先只读对接还是直接替代？（0209周会倾向于先不回填，我们替代它）
2. 企业数据库的直接访问权限尚未获取（目前只有UI界面）
3. 政策申报审核的合规性问题：从科委系统下载PDF到本地模型分析是否被允许
4. 园区GPT-企业动态的接口形式需与奇岱松确认
5. 服务匹配推荐和企业合作推荐的可行性待验证（依赖服务资源清单和产业链知识）

## Phase 1 产出完成

在需求池完善后，继续输出了 Phase 1 的两个关键文档：

### 需求分析报告 (`docs/02-需求分析/需求分析报告.md`)

- 118 条需求分类：78 条明确需求 / 33 条隐含需求 / 7 条伪需求
- 按场景详析了用户需求来源（谁说了什么）
- 识别了 7 项遗漏（权限管理、数据更新频率、方言识别、搜索检索、移动端、通知、导出）
- 识别了 6 项风险（员工抵触录音、DB 权限、烽火冲突、政策标准变化、数据稀少、工期压力）
- 提出 P0 核心路径：企业数据库 → 企业画像 → 背调/初筛/订单匹配
- 标出 5 个最不确定的点待 FDE 审查

### 澄清问题清单 (`docs/02-需求分析/澄清问题清单.md`)

- 3 个最关键问题：① 烽火对接还是替代 ② 企业画像是否三场景共用 ③ 4月是Demo还是真实可用
- 5 个补充问题：政策×走访流程合并、需求处理模块范围、运营监控程度、面向企业功能、数据更新机制
- 12 项待收集数据/文档清单（多数未获取）
- 5 项需了解的系统现状

### Phase 2 直接推进（基于假设）

FDE 指示不等客户反馈，基于推荐选项作为假设直接进 Phase 2。

**采用的假设决策**：
1. 烽火CRM → 替代（不回填）
2. 企业画像 → 三场景共用一套
3. 4月交付 → 重点功能真实可用，其余mock
4. 政策×走访 → 合并
5. 需求处理 → 一期简版
6. 运营监控 → 先做软件可达的
7. 面向企业 → 一期仅内部
8. 数据更新 → AI提取→人工确认→入库

**Phase 2 产出**：
- `场景分析-企业走访.md` — 4阶段流程：拜访准备→实施→复盘→需求闭环
- `场景分析-政策服务.md` — 4阶段流程：企业筛选→触达→诊断→结果跟踪
- `场景分析-孵化器管理.md` — 2+1阶段：运营监控→订单匹配→(成长评估延后)
- `数据需求清单.md` — 全量数据依赖汇总，按阻塞优先级排序

**关键发现**：
- 🔴 企业数据库DB权限是全局阻塞项
- 🔴 孵化企业清单+BP是孵化器场景阻塞项
- 架构建议：上下文平台（数据层）+ 企业画像（公共UI）+ 3个场景App（业务层）
- 三个场景在飞书妙记解析、飞书任务分发、deep research上有共用能力

### Phase 3 产品设计完成

继续完成 Phase 3，产出放在 `docs/03-产品设计/` 下：

**产品功能架构** (`产品功能架构.md`)：
- 总体架构：App层 + Agent层 + 数据层三层架构
- 公共企业画像模块：三个App共享一套企业画像页（不同Tab）
- 数据模型设计：6张核心表（enterprises/visit_records/background_reports/policy_assessments/incubator_matches/system_configs）+ 3张扩展表
- 迭代路线：0.1骨架 → 0.2数据闭环 → 0.3补全体验

**三个App设计**：
- `客户拜访App.md`：6页（企业列表→画像→背调→走访列表→走访详情→配置）
- `政策服务App.md`：7页（工作台→企业大盘→政策大盘→筛选→任务→诊断→看板）
- `孵化器管理App.md`：5页（概览→企业列表→匹配对话→关系图→看板）

**Agent设计** (`Agent设计汇总.md`)：
- 6个Agent：背调报告/走访记录/政策初筛/政策诊断/订单匹配/Deep Research
- 每个Agent有完整的执行步骤、工具调用、输出格式和测试场景

**数据接入方案** (`数据接入方案.md`)：
- 8个数据源的接入方式和步骤
- 同步策略（实时/定时/手动）
- 0.1版最小数据集需求

### Phase 4 静态 Demo 完成

从 `skills/north-app/app-template/` 复制脚手架，搭建了三个 App 的静态 Demo：

**visit-app（客户拜访）** `apps/visit-app/` port:3333
- schema.ts: Enterprise/VisitRecord/BackgroundReport/VisitDemand 完整类型
- mock-data.ts: 5家企业 + 4条走访 + 1份背调报告 + 4条需求
- 首页: 统计卡片 + 搜索筛选 + 企业列表
- 企业画像详情页: 3 Tab（基本信息/走访时间线/需求清单）
- 背调报告页: 左侧8模块折叠报告 + 右侧沟通清单（洞察+提纲+必问问题）

**policy-app（政策服务）** `apps/policy-app/` port:3334
- schema.ts: PolicyAssessment/ScreeningDetail/PolicyRule 完整类型
- mock-data.ts: 8条高新认定rubrics + 4家企业评估 + PM进度 + 统计
- 首页: 统计概览 + 触达漏斗 + PM进度表 + 筛选结果列表（按分级筛选）
- 初筛报告页: 综合评级卡片 + 逐项条件展示（pass/fail/pending + 置信度）

**incubator-app（孵化器管理）** `apps/incubator-app/` port:3335
- schema.ts: IncubatorEnterprise/MatchResult/RelationshipGraph/ChatMessage 完整类型
- mock-data.ts: 5家孵化企业 + 自动洗车完整匹配案例 + 活跃度报告
- 首页: 概况卡片 + 活跃度排行 + 企业列表
- 订单匹配页: 左侧对话式交互（预置引导+对话气泡）+ 右侧匹配结果面板（排名卡片+组合建议）

### Phase 4 UI 打磨 — 全面重写

FDE 反馈现有页面问题：页面看不出主要功能和操作动线、企业画像不完整、飞书联动没展示、UI太简单没设计感、有AI模板感、与产品设计文档不一致。

**逐页对照产品设计文档重写了全部 7 个页面：**

**visit-app 3 个页面：**
1. **首页** — 待办提醒条（待确认走访+待处理需求）+ 5 格数据概览 + 企业表格（名称/赛道/阶段/规模/走访时间）+ 右侧最近走访动态
2. **企业画像详情页** — 企业概要头部（标签+关键指标+飞书文档链接）+ 4 Tab（基本信息/走访记录/需求清单/服务记录）。基本信息包含完整 8 个模块（工商/产业/股权/融资/团队/产品/动态/产业链），数据来源标注。走访记录用部门颜色区分（产促蓝/科创绿/园发紫/孵化黄），展示飞书妙记关联状态。需求清单表格化展示。
3. **背调报告页** — 面包屑导航 + 报告状态标签 + 飞书文档操作（在飞书中打开/导出飞书文档/重新生成）+ 左侧 8 模块报告（带序号+数据来源颜色标签）+ 右侧沟通清单（关键洞察/谈话提纲/必问问题带分类标签）

**policy-app 2 个页面：**
1. **政策工作台** — 6 格统计概览（已筛选/A级/B级/C级/已触达/有意愿）+ 触达漏斗（6级+说明文字）+ PM 进度表（含进度条）+ 筛选结果表格（级别/企业名/评分/通过率/触达状态/负责人）+ 操作按钮（一键分发A级/新一轮筛选）
2. **初筛报告页** — 综合评级卡片（带评分条+缺失数据提示）+ 逐项条件评估（左侧颜色条区分通过/不通过/待确认，硬性/软性标签，企业数据 vs 要求标准对比，数据来源+置信度）+ 飞书任务分发按钮

**incubator-app 2 个页面：**
1. **运营概览** — 5 格概况（在孵/员工/高活跃/需关注/平均活跃度）+ 孵化企业列表（产品+技术栈标签+BP摘要+工位+入孵时间）+ 活跃度排行（进度条+信号说明）
2. **订单匹配页** — 对话式交互（引导卡片+对话气泡，去掉emoji用文字）+ 右侧匹配结果面板（推荐组合卡片+排名卡片带匹配度/活跃度/位置/产品标签）

**UI 设计原则改进：**
- 去掉统一 indigo 色，每个 App 有自己的主色（拜访=蓝、政策=绿、孵化=紫）
- 去掉圆角按钮圆角卡片模板感，改为扁平边框+表格化布局
- 统一使用 11px uppercase tracking-wider 标签，tabular-nums 数字
- 数据来源标注、飞书联动状态、任务状态等全部在 UI 中体现
- 面包屑导航取代返回箭头，操作按钮明确标注功能

### Phase 4 UI 二次打磨 — shadcn/ui + Lucide 图标

FDE 反馈 UI 仍然太丑，缺乏设计感。参考 `skills/north-app/references/` 的设计指南重做：

**安装 UI 基础设施：**
- 三个 App 统一安装 `lucide-react`（图标）、`clsx` + `tailwind-merge`（cn()工具）、`class-variance-authority`（组件变体）、`tailwindcss-animate`（动效）
- 创建 `src/lib/utils.ts` — cn() 工具函数
- 更新 `src/index.css` — CSS 变量主题系统（带微妙蓝调的灰色 `hsl(220,10%,...)`），多层自然阴影（shadow-subtle / shadow-elevated / shadow-elegant）
- 更新 `tailwind.config.js` — CSS 变量颜色映射 + tailwindcss-animate 插件 + 自定义动画

**遵循 anti-ai-design.md 指南的改进：**
- 所有 SVG 内联图标替换为 **Lucide 图标组件**（Building2, Users, FileText, ChevronRight, ExternalLink 等）
- 颜色改用 **CSS 变量语义色**（`text-foreground`, `bg-card`, `border-border`, `text-muted-foreground`）
- 卡片使用 **多层阴影**（shadow-subtle 默认 → hover:shadow-elevated 提升）
- 所有可交互元素加 **微交互**（`hover:-translate-y-px hover:shadow-md active:translate-y-0`，150ms ease-out）
- 头部加 **毛玻璃效果**（`backdrop-blur supports-[backdrop-filter]:bg-card/80`）
- 表格使用 `<table>` 原生语义标签替代 div grid
- 字体反锯齿 `antialiased` + `font-feature-settings`
- 每个 App 保持自己的主色（拜访=primary蓝、政策=emerald-700、孵化=purple-700）

**7 个页面全部用新体系重写，编译通过。**

### Phase 4 架构重构 — 统一门户 + 飞书设计

FDE 反馈：三个App分开太散、企业画像应独立、参考飞书设计。

**架构变更：**
- 三个独立 App (visit/policy/incubator) → **一个统一门户** (visit-app, port:3333)
- 三个 schema + mock-data → **合并到一个文件**
- 企业画像从"客户拜访的子页面" → **独立为一级模块**

**飞书风格侧边栏导航：**
```
漕河泾驾驶舱
├── 工作台（Dashboard）        /
├── 企业画像库（独立模块）      /enterprises
│   ├── 企业详情                /enterprises/[id]
│   └── 背调报告                /enterprises/[id]/report
├── 客户拜访                    /visit
├── 政策服务                    /policy
│   └── 初筛报告                /policy/screening/[id]
└── 孵化器管理                  /incubator
    └── 订单匹配                /incubator/match
```

**飞书设计语言：**
- 侧边栏: 240px 宽, `#f7f8fa` 背景, 可折叠到 60px
- 主色: `#3370ff`（飞书蓝）, 激活项蓝色高亮
- 文本色: `#1f2329`(主) / `#646a73`(次) / `#8f959e`(辅)
- 边框: `#dee0e3`, 浅分隔: `#f0f1f5`
- 背景: 页面 `#f5f6f8`, 卡片 `#ffffff`
- 警告: `#fef8e8` 背景 + `#dc8c07` 文字
- 成功: `#34c724`, 错误: `#f54a45`, 紫色: `#7b67ee`
- 无装饰性阴影和渐变，纯扁平边框分隔

**10 个页面全部创建完成，编译通过，所有路由返回 200。**

### 下一步

Phase 4 统一门户完成。后续：
- FDE 打开 `http://localhost:3333` 查看效果
- policy-app 和 incubator-app 可以停掉了，所有功能在 visit-app 统一门户中
- Phase 5: 接真实数据 + Agent 集成
- Phase 6: 数据接入 + Agent 工作空间配置 + 联调

## 数据来源

- `docs/01-客户资料/场景一/漕河泾1—企业走访场景.txt`（1h16m会议纪要）
- `docs/01-客户资料/场景二/场景3——孵化器.txt`（1h23m会议纪要）
- `docs/01-客户资料/场景三/场景3——政策匹配.txt`（1h20m会议纪要）
- `docs/01-客户资料/场景一/场景一_企业走访_流程图提取.md`
- `docs/01-客户资料/场景二/场景二_政策服务_流程图提取.md`
- `docs/01-客户资料/场景三/场景三_孵化器管理_流程图提取.md`
- `docs/01-客户资料/项目周会/0204周会.md`
- `docs/01-客户资料/项目周会/0209周会.md`
