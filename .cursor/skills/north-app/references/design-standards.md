---
name: design-standards
description: "设计标准体系 - 建立统一的设计语言和规范，包含设计令牌、色彩系统、排版系统、间距系统等完整规范。"
version: 1.0.0
category: design
---

# 设计标准体系

> 标准化是高端设计的基础 — 通过统一的设计语言降低认知负担，建立品牌一致性

---

## 一、设计令牌系统（Design Tokens）

### 什么是设计令牌

**定义**：设计令牌是设计系统的原子级变量，是连接设计与代码的桥梁。

**价值**：
- 🎯 **一致性**：确保整个产品视觉统一
- 🔄 **可维护**：一处修改，全局生效
- 🌓 **主题支持**：轻松实现深色模式等主题切换
- 🤝 **设计-开发协作**：统一的语言体系

### 令牌分层架构

```
┌─────────────────────────────────────────────────────────────────┐
│                      语义层（Semantic）                          │
│   --color-text-primary, --color-bg-elevated, --button-radius    │
├─────────────────────────────────────────────────────────────────┤
│                      别名层（Alias）                             │
│   --color-blue-600, --spacing-lg, --font-size-xl               │
├─────────────────────────────────────────────────────────────────┤
│                      基础层（Primitive）                         │
│   --blue-600: hsl(220, 60%, 50%)                               │
│   --spacing-6: 1.5rem                                          │
│   --font-size-20: 1.25rem                                      │
└─────────────────────────────────────────────────────────────────┘
```

### CSS变量定义示例

```css
/* tokens/primitives.css - 基础层 */
:root {
  /* 色彩基础值 */
  --blue-50: hsl(220, 60%, 97%);
  --blue-100: hsl(220, 60%, 94%);
  --blue-200: hsl(220, 60%, 88%);
  --blue-300: hsl(220, 60%, 78%);
  --blue-400: hsl(220, 60%, 65%);
  --blue-500: hsl(220, 60%, 50%);
  --blue-600: hsl(220, 60%, 45%);
  --blue-700: hsl(220, 60%, 38%);
  --blue-800: hsl(220, 60%, 30%);
  --blue-900: hsl(220, 60%, 20%);
  
  /* 中性色 - 带微妙蓝调，避免死灰 */
  --gray-50: hsl(220, 10%, 98%);
  --gray-100: hsl(220, 10%, 96%);
  --gray-200: hsl(220, 10%, 92%);
  --gray-300: hsl(220, 10%, 85%);
  --gray-400: hsl(220, 10%, 65%);
  --gray-500: hsl(220, 10%, 50%);
  --gray-600: hsl(220, 10%, 40%);
  --gray-700: hsl(220, 10%, 30%);
  --gray-800: hsl(220, 10%, 20%);
  --gray-900: hsl(220, 10%, 10%);
  
  /* 间距基础值 */
  --space-1: 0.25rem;   /* 4px */
  --space-2: 0.5rem;    /* 8px */
  --space-3: 0.75rem;   /* 12px */
  --space-4: 1rem;      /* 16px */
  --space-5: 1.25rem;   /* 20px */
  --space-6: 1.5rem;    /* 24px */
  --space-8: 2rem;      /* 32px */
  --space-10: 2.5rem;   /* 40px */
  --space-12: 3rem;     /* 48px */
  --space-16: 4rem;     /* 64px */
  --space-20: 5rem;     /* 80px */
  --space-24: 6rem;     /* 96px */
  
  /* 字号基础值 */
  --text-xs: 0.75rem;   /* 12px */
  --text-sm: 0.875rem;  /* 14px */
  --text-base: 1rem;    /* 16px */
  --text-lg: 1.125rem;  /* 18px */
  --text-xl: 1.25rem;   /* 20px */
  --text-2xl: 1.5rem;   /* 24px */
  --text-3xl: 1.875rem; /* 30px */
  --text-4xl: 2.25rem;  /* 36px */
  --text-5xl: 3rem;     /* 48px */
  --text-6xl: 3.75rem;  /* 60px */
  
  /* 圆角基础值 */
  --radius-none: 0;
  --radius-sm: 0.25rem;  /* 4px */
  --radius-md: 0.5rem;   /* 8px */
  --radius-lg: 0.75rem;  /* 12px */
  --radius-xl: 1rem;     /* 16px */
  --radius-2xl: 1.5rem;  /* 24px */
  --radius-full: 9999px;
}

/* tokens/semantic.css - 语义层 */
:root {
  /* 文本颜色 */
  --color-text-primary: var(--gray-900);
  --color-text-secondary: var(--gray-600);
  --color-text-tertiary: var(--gray-500);
  --color-text-inverse: white;
  --color-text-link: var(--blue-600);
  
  /* 背景颜色 */
  --color-bg-page: var(--gray-50);
  --color-bg-surface: white;
  --color-bg-elevated: white;
  --color-bg-sunken: var(--gray-100);
  
  /* 边框颜色 */
  --color-border-default: var(--gray-200);
  --color-border-strong: var(--gray-300);
  --color-border-focus: var(--blue-500);
  
  /* 交互颜色 */
  --color-interactive-primary: var(--blue-600);
  --color-interactive-primary-hover: var(--blue-700);
  --color-interactive-secondary: var(--gray-100);
  --color-interactive-secondary-hover: var(--gray-200);
  
  /* 语义颜色 */
  --color-success: hsl(145, 50%, 45%);
  --color-warning: hsl(35, 80%, 50%);
  --color-error: hsl(0, 60%, 50%);
  --color-info: var(--blue-500);
  
  /* 组件特定 */
  --button-radius: var(--radius-md);
  --card-radius: var(--radius-lg);
  --input-radius: var(--radius-md);
  --modal-radius: var(--radius-xl);
}

/* 深色模式 */
.dark {
  --color-text-primary: var(--gray-100);
  --color-text-secondary: var(--gray-400);
  --color-text-tertiary: var(--gray-500);
  
  --color-bg-page: var(--gray-900);
  --color-bg-surface: var(--gray-800);
  --color-bg-elevated: var(--gray-700);
  --color-bg-sunken: var(--gray-900);
  
  --color-border-default: var(--gray-700);
  --color-border-strong: var(--gray-600);
  
  --color-interactive-primary: var(--blue-500);
  --color-interactive-primary-hover: var(--blue-400);
}
```

---

## 二、色彩系统

### 色彩哲学

**高端优雅的色彩原则**：

| 原则 | 说明 | 实践 |
|-----|------|------|
| **克制** | 少即是多 | 1个主色 + 1个强调色 |
| **低饱和** | 避免刺眼 | 饱和度降低20-30% |
| **统一色调** | 和谐整体 | 所有颜色基于同一色调 |
| **语义清晰** | 颜色即含义 | 红=错误，绿=成功 |

### 色彩选择指南

#### 主色选择

```yaml
选择主色的考量:
  品牌契合:
    - 是否符合品牌调性？
    - 是否传达正确的情感？
    
  实用性:
    - 是否在各种背景下可读？
    - 是否有足够的色阶空间？
    
  差异化:
    - 是否避开行业同质化？
    - 是否有独特的识别度？

推荐的高端主色:
  深邃蓝: "hsl(220, 60%, 50%)"  # 专业、可信
  优雅灰: "hsl(220, 10%, 25%)"  # 现代、克制
  沉稳绿: "hsl(160, 40%, 40%)"  # 自然、健康
  温暖橙: "hsl(25, 70%, 55%)"   # 活力、友好
  
避免的AI味色彩:
  ❌ 紫色渐变 "linear-gradient(135deg, #667eea, #764ba2)"
  ❌ 高饱和蓝 "hsl(220, 100%, 50%)"
  ❌ 霓虹绿 "hsl(120, 100%, 50%)"
```

#### 中性色设计

```css
/* 好的中性色 - 带微妙色调 */
.neutral-warm {
  /* 暖灰 - 适合温馨、亲和的产品 */
  --gray-50: hsl(30, 10%, 98%);
  --gray-500: hsl(30, 5%, 50%);
  --gray-900: hsl(30, 10%, 10%);
}

.neutral-cool {
  /* 冷灰 - 适合专业、科技的产品 */
  --gray-50: hsl(220, 10%, 98%);
  --gray-500: hsl(220, 5%, 50%);
  --gray-900: hsl(220, 10%, 10%);
}

/* 避免的中性色 */
.neutral-dead {
  /* 纯灰 - 缺乏生命力 */
  --gray-500: hsl(0, 0%, 50%);  /* ❌ 死气沉沉 */
}
```

### 色彩对比度标准

```yaml
WCAG 2.1 AA标准:
  正常文本: "至少4.5:1对比度"
  大文本: "至少3:1对比度（≥18px或≥14px bold）"
  UI组件: "至少3:1对比度"

实践建议:
  - 正文文本使用 gray-900 在白色背景
  - 次要文本最低使用 gray-600
  - 禁用状态使用 gray-400 但需其他视觉指示
```

---

## 三、排版系统

### 字体选择

```yaml
字体配对原则:
  对比:
    - 衬线 + 无衬线 = 经典组合
    - 几何 + 人文 = 现代组合
    
  功能:
    - 展示字体（Display）: 标题、引用
    - 正文字体（Body）: 段落、说明
    - 等宽字体（Mono）: 代码、数据

推荐字体配对:
  高端优雅:
    display: "'Instrument Serif', Georgia"
    body: "'Satoshi', 'SF Pro Text'"
    
  现代科技:
    display: "'Space Grotesk', 'SF Pro Display'"
    body: "'Inter var', system-ui"
    
  温暖友好:
    display: "'Fraunces', 'Playfair Display'"
    body: "'Source Sans Pro', 'Nunito'"

避免的字体:
  ❌ Inter（过度使用，已成AI标志）
  ❌ Roboto（过于通用）
  ❌ Arial（无个性）
  ❌ 系统默认字体堆栈作为唯一选择
```

### 字号比例系统

```yaml
字号系统设计:
  比例选择:
    小型产品: "1.2（小三度）"
    中型产品: "1.25（大二度）"
    大型产品: "1.333（完美四度）"
  
  以1.25比例为例:
    base: "16px"
    计算: "16 × 1.25 = 20, 20 × 1.25 = 25..."
    
  标准字号梯度:
    xs: "12px"    # 标签、辅助信息
    sm: "14px"    # 次要文本、表格内容
    base: "16px"  # 正文基准
    lg: "18px"    # 强调文本
    xl: "20px"    # 小标题
    2xl: "24px"   # 中标题
    3xl: "30px"   # 大标题
    4xl: "36px"   # 特大标题
    5xl: "48px"   # 页面主标题
```

### 行高与字间距

```css
/* 行高系统 */
:root {
  --leading-none: 1;        /* 标题压缩 */
  --leading-tight: 1.25;    /* 标题舒展 */
  --leading-snug: 1.375;    /* 副标题 */
  --leading-normal: 1.5;    /* 短文本 */
  --leading-relaxed: 1.625; /* 正文 */
  --leading-loose: 1.75;    /* 大段落 */
}

/* 应用规则 */
h1, h2 {
  line-height: var(--leading-tight);
  letter-spacing: -0.02em; /* 大字号收紧字间距 */
}

p, li {
  line-height: var(--leading-relaxed);
  letter-spacing: normal;
}

.caption, .label {
  line-height: var(--leading-normal);
  letter-spacing: 0.01em; /* 小字号微微展开 */
}
```

### 排版最佳实践

```yaml
段落设计:
  最大宽度: "65-75字符（约650px）"
  段间距: "行高 × 1.5"
  首行缩进: "不推荐，使用段间距替代"

标题层级:
  规则: "相邻层级字号差距至少1.2倍"
  示例:
    h1: "36px"
    h2: "30px"  # 36 ÷ 1.2 = 30
    h3: "24px"  # 30 ÷ 1.25 = 24
    
避免:
  ❌ 全部使用相同字重
  ❌ 行高过紧（< 1.4）
  ❌ 段落过宽（> 80字符）
  ❌ 字号跳跃过大
```

---

## 四、间距系统

### 8px网格系统

```yaml
为什么选择8px:
  - 能被2、4整除，适合各种尺寸
  - 与常见屏幕尺寸兼容
  - 心理学研究显示8的倍数视觉和谐

间距池:
  4px:  "微小间距 - 紧密元素内部"
  8px:  "小间距 - 相关元素之间"
  12px: "中小间距 - 组件内部padding"
  16px: "基础间距 - 标准padding"
  24px: "中间距 - 区块分隔"
  32px: "大间距 - 版块分隔"
  48px: "特大间距 - 页面区域"
  64px: "超大间距 - 主要区域"
```

### 间距应用规则

```css
/* 组件内部间距 */
.card {
  padding: var(--space-6);  /* 24px - 宽松的内部空间 */
}

.card-compact {
  padding: var(--space-4);  /* 16px - 紧凑的内部空间 */
}

/* 元素之间间距 */
.stack > * + * {
  margin-top: var(--space-4);  /* 16px - 堆叠元素间距 */
}

.stack-loose > * + * {
  margin-top: var(--space-6);  /* 24px - 宽松堆叠 */
}

/* 版块间距 */
.section + .section {
  margin-top: var(--space-16);  /* 64px - 版块分隔 */
}

/* 页面边距 */
.container {
  padding-inline: var(--space-4);  /* 移动端 */
}

@media (min-width: 768px) {
  .container {
    padding-inline: var(--space-8);  /* 平板及以上 */
  }
}
```

### 间距层级关系

```
┌─────────────────────────────────────────────────────────────────┐
│  页面层（Page）                                                 │
│  padding: 64px                                                  │
│  ┌─────────────────────────────────────────────────────────────┐
│  │  版块层（Section）                                          │
│  │  margin-bottom: 48px                                        │
│  │  ┌───────────────────────────────────────────────────────┐  │
│  │  │  组层（Group）                                        │  │
│  │  │  gap: 24px                                            │  │
│  │  │  ┌─────────────────────────────────────────────────┐  │  │
│  │  │  │  组件层（Component）                            │  │  │
│  │  │  │  padding: 16px                                  │  │  │
│  │  │  │  ┌───────────────────────────────────────────┐  │  │  │
│  │  │  │  │  元素层（Element）                        │  │  │  │
│  │  │  │  │  gap: 8px                                 │  │  │  │
│  │  │  │  └───────────────────────────────────────────┘  │  │  │
│  │  │  └─────────────────────────────────────────────────┘  │  │
│  │  └───────────────────────────────────────────────────────┘  │
│  └─────────────────────────────────────────────────────────────┘
└─────────────────────────────────────────────────────────────────┘
```

---

## 五、圆角系统

### 圆角规范

```yaml
圆角层级:
  none: "0 - 直角，用于表格、分隔线"
  sm: "4px - 输入框、小按钮、标签"
  md: "8px - 按钮、卡片、弹窗"
  lg: "12px - 大卡片、区块"
  xl: "16px - 模态框、面板"
  2xl: "24px - 装饰性元素"
  full: "9999px - 胶囊形状、头像"

应用原则:
  一致性: "同类元素使用相同圆角"
  层级感: "内层元素圆角 < 外层元素圆角"
  功能性: "交互元素适当圆角更友好"
```

### 圆角嵌套规则

```css
/* 嵌套圆角公式：内圆角 = 外圆角 - padding */

.card {
  border-radius: var(--radius-lg);  /* 12px */
  padding: var(--space-4);          /* 16px */
}

.card .button {
  /* 理论值: 12px - 16px < 0，所以使用最小值 */
  border-radius: var(--radius-sm);  /* 4px */
}

/* 更大padding的情况 */
.modal {
  border-radius: var(--radius-xl);  /* 16px */
  padding: var(--space-6);          /* 24px */
}

.modal .card {
  /* 16px - 24px < 0，内部元素使用较小圆角 */
  border-radius: var(--radius-md);  /* 8px */
}
```

---

## 六、阴影系统

### 高级阴影设计

```css
/* 单层阴影 - 简单但缺乏层次 */
.shadow-basic {
  box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
}

/* 多层阴影 - 更自然、更高级 */
.shadow-elegant {
  box-shadow: 
    /* 紧贴的暗边 */
    0 1px 2px rgba(0, 0, 0, 0.04),
    /* 中层扩散 */
    0 4px 8px rgba(0, 0, 0, 0.04),
    /* 远处模糊 */
    0 12px 24px rgba(0, 0, 0, 0.06);
}

/* 阴影系统 */
:root {
  /* 悬浮层级 - 从低到高 */
  --shadow-xs: 0 1px 2px rgba(0, 0, 0, 0.05);
  
  --shadow-sm: 
    0 1px 2px rgba(0, 0, 0, 0.04),
    0 2px 4px rgba(0, 0, 0, 0.04);
  
  --shadow-md: 
    0 2px 4px rgba(0, 0, 0, 0.03),
    0 4px 8px rgba(0, 0, 0, 0.04),
    0 8px 16px rgba(0, 0, 0, 0.04);
  
  --shadow-lg: 
    0 4px 6px rgba(0, 0, 0, 0.02),
    0 10px 20px rgba(0, 0, 0, 0.04),
    0 20px 40px rgba(0, 0, 0, 0.06);
  
  --shadow-xl: 
    0 8px 16px rgba(0, 0, 0, 0.03),
    0 20px 40px rgba(0, 0, 0, 0.05),
    0 40px 80px rgba(0, 0, 0, 0.08);
}
```

### 阴影应用场景

```yaml
阴影使用指南:
  xs: "微弱悬浮 - 下拉菜单、工具提示"
  sm: "轻微悬浮 - 卡片默认状态"
  md: "明显悬浮 - 卡片悬停、弹出层"
  lg: "高度悬浮 - 模态框、侧边栏"
  xl: "最高层级 - 全屏覆盖、toast"

避免:
  ❌ 阴影过重（透明度 > 0.2）
  ❌ 模糊半径过大造成的"发光"效果
  ❌ 硬边阴影（无模糊）
  ❌ 彩色阴影（除非有特殊设计意图）
```

---

## 七、动效系统

### 时长规范

```css
:root {
  /* 时长 - 基于交互类型 */
  --duration-instant: 0ms;     /* 无延迟 */
  --duration-fast: 100ms;      /* 微交互 */
  --duration-normal: 200ms;    /* 标准过渡 */
  --duration-slow: 300ms;      /* 复杂动画 */
  --duration-slower: 500ms;    /* 大型变化 */
}
```

### 缓动函数

```css
:root {
  /* 缓动 - 物理运动感 */
  --ease-linear: linear;
  --ease-in: cubic-bezier(0.4, 0, 1, 1);
  --ease-out: cubic-bezier(0, 0, 0.2, 1);
  --ease-in-out: cubic-bezier(0.4, 0, 0.2, 1);
  
  /* 弹性 - 活泼感 */
  --ease-spring: cubic-bezier(0.175, 0.885, 0.32, 1.275);
  
  /* 强调 - 戏剧性 */
  --ease-emphasized: cubic-bezier(0.2, 0, 0, 1);
}
```

### 动效应用规则

```yaml
动效原则:
  功能性:
    - 动效应有明确目的（反馈、引导、过渡）
    - 不为炫技而动效
    
  克制:
    - 持续时间不超过300ms
    - 避免同时多处动效
    
  一致性:
    - 同类操作使用相同动效
    - 相关元素动效协调
    
  可访问性:
    - 尊重 prefers-reduced-motion
    - 关键信息不依赖动效传达

常见动效配置:
  hover:
    duration: "100-150ms"
    easing: "ease-out"
    properties: "opacity, background-color, transform"
    
  focus:
    duration: "150ms"
    easing: "ease-out"
    properties: "box-shadow, outline"
    
  modal:
    duration: "200-300ms"
    easing: "ease-in-out"
    properties: "opacity, transform"
```

---

## 八、标准检查清单

### 设计令牌检查

- [ ] 所有颜色定义在CSS变量中
- [ ] 颜色支持深色模式切换
- [ ] 间距遵循8px网格
- [ ] 字号梯度合理（1.2-1.333比例）
- [ ] 圆角系统统一
- [ ] 阴影分层自然

### 视觉一致性检查

- [ ] 同类元素视觉风格一致
- [ ] 颜色对比度符合WCAG AA
- [ ] 排版层级清晰
- [ ] 间距节奏和谐
- [ ] 动效时长一致

### 代码质量检查

- [ ] 变量命名语义化
- [ ] 无魔法数字（硬编码值）
- [ ] CSS组织结构清晰
- [ ] 支持响应式

---

## 参考资源

- Material Design 3
- Apple Human Interface Guidelines
- Tailwind CSS Design System
- Radix UI Design Principles
